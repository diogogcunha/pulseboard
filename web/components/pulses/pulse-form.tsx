'use client';

// TODO (Issue #4, #5): Implement full pulse creation form
// - Title, question, response type selector (scale/emoji/text), team selector
// - Call POST /api/v1/pulses on submit
export function PulseForm() {
  return (
    <div className="bg-white rounded-lg border p-6">
      <p className="text-gray-500 text-sm text-center">
        Pulse creation form — see Issue #5 to implement
      </p>
    </div>
  );
}
